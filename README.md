# МОДУЛЬ 2
### СПРАВОЧНИК ФОРМА СПИСКА, СОЗДАНИЕ КОМАНДЫ "СЧИТАТЬ ИЗ JSON".
КОД:
```
&НаСервере
Процедура СчитатьИзJSONНаСервере()
	ПутьКФайлу = "C:\Users\Admin\Desktop\Заказчики.json";     
	ЧтениеJSON = Новый ЧтениеJSON;
	ЧтениеJSON.ОткрытьФайл(ПутьКФайлу);  
	Сообщить(ЧтениеJSON);
	ДанныеИзФайла = ПрочитатьJSON(ЧтениеJSON);  
	ЧтениеJSON.Закрыть();
	Для каждого Данные из ДанныеИзФайла цикл
		СпрЗаказчики = Справочники.Заказчики.НайтиПоКоду(Данные.id);
		Если СпрЗаказчики.Пустая() Тогда
			СпрЗаказчики = Справочники.Заказчики.СоздатьЭлемент();
			СпрЗаказчики.Наименование = Данные.name;
			СпрЗаказчики.ИНН = Данные.inn;
			СпрЗаказчики.Адрес = Данные.addres;
			СпрЗаказчики.Телефон = Данные.phone;
			СпрЗаказчики.ЯвляетсяПродавцом = Данные.salesman;
			СпрЗаказчики.ЯвляетсяПокупателем = Данные.buyer;
			СпрЗаказчики.Записать();  
		КонецЕсли;
	КонецЦикла;	
КонецПроцедуры

&НаКлиенте
Процедура СчитатьИзJSON(Команда)
	СчитатьИзJSONНаСервере();
КонецПроцедуры
```


---

# МОДУЛЬ 3
### МОДУЛЬ ОБЪЕКТА ДОКУМЕНТА

```
Процедура ПередЗаписью(Отказ, РежимЗаписи, РежимПроведения)
    Если Состав.Количество() > 0 Тогда
        РассчитатьПолнуюСтоимость();
    КонецЕсли;
КонецПроцедуры

Процедура РассчитатьПолнуюСтоимость() Экспорт
    СтоимостьПродукции = Состав.Итог("Сумма");

    Запрос = Новый Запрос;
    Запрос.Текст =
    "ВЫБРАТЬ
    |	СоставЗаказа.Продукция КАК Продукция,
    |	СоставЗаказа.Количество КАК КоличествоПродукции,
    |	СоставСпецификации.Материал КАК Материал,
    |	СоставСпецификации.Количество КАК НормаРасхода,
    |	ЕСТЬNULL(ЦМ.Цена, 0) КАК ЦенаМатериала,
    |	СоставСпецификации.Количество * СоставЗаказа.Количество * ЕСТЬNULL(ЦМ.Цена, 0) КАК СтоимостьМатериала
    |ИЗ
    |	Документ.ЗаказПокупателя.Состав КАК СоставЗаказа
    |		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.Спецификации КАК Спецификации
    |		ПО СоставЗаказа.Продукция = Спецификации.Продукция
    |		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.Спецификации.Состав КАК СоставСпецификации
    |		ПО (СоставСпецификации.Ссылка = Спецификации.Ссылка)
    |		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.ЦеныМатериалов КАК ЦМ
    |		ПО (СоставСпецификации.Материал = ЦМ.Материал)
    |ГДЕ
    |	СоставЗаказа.Ссылка = &СсылкаЗаказа";

    Запрос.УстановитьПараметр("СсылкаЗаказа", ЭтотОбъект.Ссылка);

    Результат = Запрос.Выполнить();

    СтоимостьМатериалов = 0;

    Если НЕ Результат.Пустой() Тогда
        Выборка = Результат.Выбрать();
        Пока Выборка.Следующий() Цикл
            СтоимостьМатериалов = СтоимостьМатериалов + Выборка.СтоимостьМатериала;
        КонецЦикла;
    КонецЕсли;

    Итого = СтоимостьПродукции + СтоимостьМатериалов;
КонецПроцедуры
```


---

# МОДУЛЬ 4
### СОЗДАНИЕ ОБЪЕКТОВ СПРАВОЧНИКА "ПОЛЬЗОВАТЕЛИ".
РЕКВИЗИТЫ - `"ПАРОЛЬ", "КОЛИЧЕСТВО ПОПЫТОК", "ДАТА ВХОДА", "ЗАБЛОКИРОВАН", "РОЛЬ"`.
### СОЗДАНИЕ ПЕРЕЧИСЛЕНИЯ `"РОЛИ ПОЛЬЗОВАТЕЛЕЙ".`
РЕКВИЗИТЫ - `"АДМИНИСТРАТОР", "ПОЛЬЗОВАТЕЛЬ".`

### СОЗДАНИЕ ОБЩИХ МАКЕТОВ.
ТИП МАКЕТА `"ДВОИЧНЫЕ ДАННЫЕ"`, ЗАГРУЗИТЬ ИЗ ФАЙЛА - `"ПУТЬ К ФАЙЛУ".`

### СОЗДАНИЕ ОБЩИХ ФОРМ - `"ФОРМА ПОЛЬЗОВАТЕЛЯ", "ФОРМА АВТОРИЗАЦИИ"`

### ФОРМА ПОЛЬЗОВАТЕЛЯ:
ДЕКОРАЦИЯ С ТЕКСТОМ `"РАБОЧИЙ СТОЛ ПОЛЬЗОВАТЕЛЯ".`

### ФОРМА АВТОРИЗАЦИИ:

***СОЗДАНИЕ РЕКВИЗИТОВ:***

АДРЕС КАРТИНКИ 1-4, ФРАГМЕНТ ИНДЕКС 1-4, ВЫБОР ПОЛЬЗОВАТЕЛЯ, КАПЧА ПРОЙДЕНА, ЛОГИН, ОШИБКА КАПЧИ, ПАРОЛЬ, ПОЛЬЗОВАТЕЛЬ НАЙДЕН.

***СОЗДАНИЕ КОМАНДЫ:***

КНОПКА ВОЙТИ.

***ЭЛЕМЕНТЫ НА ФОРМЕ:***

ЛОГИН, ПАРОЛЬ, ДЕКОРАЦИЯ С ТЕКСТОМ "КАПЧА", ГРУППА С КАРТИНКАМИ 1-4, КНОПКА ВОЙТИ.

***СВОЙСТВА ПОЛЕЙ КАРТИНКА 1-4***

ВИД "ПОЛЕ КАРТИНКИ", ПУТЬ К ДАННЫМ - "АДРЕС КАРТИНКИ 1-4".

ГИПЕРССЫЛКА

РАЗМЕР - ПРОПОРЦИОНАЛЬНО

СОБЫТИЕ НАЖАТИЕ - КАРТИНКА 1-4 НАЖАТИЕ.

***СВОЙСТВА ГРУППЫ***

ГРУППИРОВКА ГОРИЗОНТАЛЬНАЯ

***СВОЙСТВА ФОРМА***

СОБЫТИЯ - ПРИ СОЗДАНИИ НА СЕРВЕРЕ, ПРИ ОТКРЫТИИ.

### КОД ФОРМЫ АВТОРИЗАЦИИ:
```
&НаСервере
Функция ВойтиНаСервере()
	ПользовательНайден=Справочники.Пользователи.ПустаяСсылка();
	Если НЕ Справочники.Пользователи.НайтиПоНаименованию(Логин,Истина).Пустая() Тогда
		ПользовательНайден=Справочники.Пользователи.НайтиПоНаименованию(Логин,Истина).Ссылка;
		Если ПользовательНайден.Пароль=Пароль Тогда
			ОбъектПользователя = ПользовательНайден.ПолучитьОбъект();
		    ОбъектПользователя.КоличествоПопыток = 0;
		    ОбъектПользователя.ДатаВхода = ТекущаяДата();
		    ОбъектПользователя.Записать();
			Возврат ПользовательНайден.Роль;		
		Иначе
			УстановитьКоличествоПопыток(ПользовательНайден);
			Возврат Неопределено;
		КонецЕсли;     				
	Иначе     
		Возврат Неопределено; 
	КонецЕсли;	
КонецФункции

&НаКлиенте
Процедура Войти(Команда)
	Если НЕ КапчаПройдена Тогда
        Сообщить("Пожалуйста, пройдите проверку капчи.");
        Возврат;
    КонецЕсли;	
	Роль=ВойтиНаСервере(); 
	Если НЕ Роль=Неопределено Тогда
		Если АктивностьПользователя(ПользовательНайден)=Ложь Тогда
			ИзменитьДанныеНаСервере(ПользовательНайден,"КоличествоПопыток",0);
			Если Роль=ПредопределенноеЗначение("Перечисление.РолиПользователей.Пользователь") Тогда
				ОткрытьФорму("ОбщаяФорма.ФормаПользователя");
				Сообщить("Вы успешно авторизовались");
			КонецЕсли;
			Если Роль=ПредопределенноеЗначение("Перечисление.РолиПользователей.Администратор") Тогда
				ОткрытьФорму("Справочник.Пользователи.ФормаСписка");
				Сообщить("Вы успешно авторизовались");
			КонецЕсли;	        
			ЭтаФорма.Закрыть();
		Иначе
			ИзменитьДанныеНаСервере(ПользовательНайден,"Заблокирован",Истина);
			Сообщить("Вы заблокированы. Обратитесь к администратору");
		КонецЕсли;  
	Иначе
		Если (ПопыткиПользователя(ПользовательНайден)>3) Тогда
			ИзменитьДанныеНаСервере(ПользовательНайден,"Заблокирован",Истина);
			Сообщить("Вы заблокированы. Обратитесь к администратору");	
		Иначе
		   Сообщить("Вы ввели неверный логин или пароль. Пожалуйста проверьте ещё раз введенные данные");	
	   КонецЕсли;
   КонецЕсли;
КонецПроцедуры                         

&НаСервере
Функция АктивностьПользователя(ПользовательНайден)
	Возврат ПользовательНайден.Заблокирован;
КонецФункции 

&НаСервере
Функция ВходПользователя(ПользовательНайден)
	Возврат ПользовательНайден.ДатаВхода;
КонецФункции              

&НаСервере
Функция ПопыткиПользователя(ПользовательНайден)
	Возврат ПользовательНайден.КоличествоПопыток;
КонецФункции                                     

&НаСервере
Процедура УстановитьКоличествоПопыток(Пользователь)
	ПользовательОбъект=Пользователь.ПолучитьОбъект();
	ПользовательОбъект.КоличествоПопыток=ПользовательОбъект.КоличествоПопыток+1;
	ПользовательОбъект.Записать();
КонецПроцедуры                    

&НаСервере
Процедура ИзменитьДанныеНаСервере(Пользователь,ИмяРеквизита,ЗначениеРеквизита)
	ПользовательОбъект=Пользователь.ПолучитьОбъект();
	ПользовательОбъект[ИмяРеквизита]=ЗначениеРеквизита;
	ПользовательОбъект.Записать();
КонецПроцедуры                    

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
    Фрагменты = Новый Массив;
    Фрагменты.Добавить(1);
    Фрагменты.Добавить(2);
    Фрагменты.Добавить(3);
    Фрагменты.Добавить(4);
    
    ГСЧ=Новый ГенераторСлучайныхЧисел();
	i = 3;
	Пока i > 0 Цикл
	    u=ГСЧ.СлучайноеЧисло(0, i);
	    Temp = Фрагменты[i];
	    Фрагменты[i] = Фрагменты[u];
	    Фрагменты[u] = Temp;
	    i = i - 1;
	КонецЦикла;   
	
    Фрагмент1Индекс = Фрагменты[0];
    Фрагмент2Индекс = Фрагменты[1];
    Фрагмент3Индекс = Фрагменты[2];
    Фрагмент4Индекс = Фрагменты[3];
    
    АдресКартинки1 = ПоместитьВоВременноеХранилище(
        ПолучитьОбщийМакет("МакетФрагмент" + Фрагмент1Индекс), УникальныйИдентификатор);
    АдресКартинки2 = ПоместитьВоВременноеХранилище(
        ПолучитьОбщийМакет("МакетФрагмент" + Фрагмент2Индекс), УникальныйИдентификатор);
    АдресКартинки3 = ПоместитьВоВременноеХранилище(
        ПолучитьОбщийМакет("МакетФрагмент" + Фрагмент3Индекс), УникальныйИдентификатор);
    АдресКартинки4 = ПоместитьВоВременноеХранилище(
        ПолучитьОбщийМакет("МакетФрагмент" + Фрагмент4Индекс), УникальныйИдентификатор);
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)
    КапчаПройдена=Ложь;
    ОшибкиКапчи=0;
    ВыборПользователя="";
    Элементы.ДекорацияКапча.Заголовок="Нажмите фрагменты в правильном порядке (0/4)";
КонецПроцедуры

&НаКлиенте
Процедура ПеремешатьКапчу()
    ВыборПользователя="";
    Элементы.ДекорацияКапча.Заголовок="Нажмите фрагменты в правильном порядке (0/4)";
    ПеремешатьКапчуНаСервере();
КонецПроцедуры

&НаСервере
Процедура ПеремешатьКапчуНаСервере()
    Фрагменты=Новый Массив;
    Фрагменты.Добавить(1);
    Фрагменты.Добавить(2);
    Фрагменты.Добавить(3);
    Фрагменты.Добавить(4);
    
    ГСЧ=Новый ГенераторСлучайныхЧисел();
	i=3;
	Пока i>0 Цикл
	    u= ГСЧ.СлучайноеЧисло(0,i);
	    Temp=Фрагменты[i];
	    Фрагменты[i]=Фрагменты[u];
	    Фрагменты[u]=Temp;
	    i=i-1;
	КонецЦикла;   
	
    Фрагмент1Индекс = Фрагменты[0];
    Фрагмент2Индекс = Фрагменты[1];
    Фрагмент3Индекс = Фрагменты[2];
    Фрагмент4Индекс = Фрагменты[3];
    
    АдресКартинки1 = ПоместитьВоВременноеХранилище(
        ПолучитьОбщийМакет("МакетФрагмент" + Фрагмент1Индекс), УникальныйИдентификатор);
    АдресКартинки2 = ПоместитьВоВременноеХранилище(
        ПолучитьОбщийМакет("МакетФрагмент" + Фрагмент2Индекс), УникальныйИдентификатор);
    АдресКартинки3 = ПоместитьВоВременноеХранилище(
        ПолучитьОбщийМакет("МакетФрагмент" + Фрагмент3Индекс), УникальныйИдентификатор);
    АдресКартинки4 = ПоместитьВоВременноеХранилище(
        ПолучитьОбщийМакет("МакетФрагмент" + Фрагмент4Индекс), УникальныйИдентификатор);
КонецПроцедуры

&НаСервере
Процедура ОбновитьКартинкиНаСервере(ИдФормы)
    АдресКартинки1 = ПоместитьВоВременноеХранилище(
        ПолучитьОбщийМакет("МакетФрагмент" + Фрагмент1Индекс), ИдФормы);
    АдресКартинки2 = ПоместитьВоВременноеХранилище(
        ПолучитьОбщийМакет("МакетФрагмент" + Фрагмент2Индекс), ИдФормы);
    АдресКартинки3 = ПоместитьВоВременноеХранилище(
        ПолучитьОбщийМакет("МакетФрагмент" + Фрагмент3Индекс), ИдФормы);
    АдресКартинки4 = ПоместитьВоВременноеХранилище(
        ПолучитьОбщийМакет("МакетФрагмент" + Фрагмент4Индекс), ИдФормы);
КонецПроцедуры	

&НаКлиенте
Процедура Картинка1Нажатие(Элемент, СтандартнаяОбработка)
    СтандартнаяОбработка = Ложь;
    ОбработатьВыборФрагмента(Фрагмент1Индекс);
КонецПроцедуры

&НаКлиенте
Процедура Картинка2Нажатие(Элемент, СтандартнаяОбработка)
    СтандартнаяОбработка = Ложь;
    ОбработатьВыборФрагмента(Фрагмент2Индекс);
КонецПроцедуры

&НаКлиенте
Процедура Картинка3Нажатие(Элемент, СтандартнаяОбработка)
    СтандартнаяОбработка = Ложь;
    ОбработатьВыборФрагмента(Фрагмент3Индекс);
КонецПроцедуры

&НаКлиенте
Процедура Картинка4Нажатие(Элемент, СтандартнаяОбработка)
    СтандартнаяОбработка = Ложь;
    ОбработатьВыборФрагмента(Фрагмент4Индекс);
КонецПроцедуры

&НаКлиенте
Процедура ОбработатьВыборФрагмента(Индекс)
    Если КапчаПройдена Тогда
        Возврат;
    КонецЕсли;    
    ВыборПользователя = ВыборПользователя + Строка(Индекс);
    Элементы.ДекорацияКапча.Заголовок = 
        "Нажмите фрагменты в правильном порядке (" + СтрДлина(ВыборПользователя) + "/4)";    
    Если СтрДлина(ВыборПользователя) = 4 Тогда
        ПроверитьКапчу();
    КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура ПроверитьКапчу()
    Если ВыборПользователя = "1234" Тогда
        КапчаПройдена = Истина;
        Элементы.ДекорацияКапча.Заголовок = "Капча пройдена!";
    Иначе
        ОшибкиКапчи = ОшибкиКапчи + 1;
        Если ОшибкиКапчи >= 3 Тогда
            Если НЕ ПустаяСтрока(Логин) Тогда
                ЗаблокироватьПоЛогину(Логин);
            КонецЕсли;
            Сообщить("Вы заблокированы. Обратитесь к администратору");
        Иначе
            Сообщить("Неверно! Попытка " + ОшибкиКапчи + " из 3. Попробуйте снова.");
            ВыборПользователя = "";
            ПеремешатьКапчу();
        КонецЕсли;
    КонецЕсли;
КонецПроцедуры

&НаСервере
Процедура ЗаблокироватьПоЛогину(ВходЛогин)
    Ссылка = Справочники.Пользователи.НайтиПоНаименованию(ВходЛогин, Истина);
    Если НЕ Ссылка.Пустая() Тогда
        Объект = Ссылка.ПолучитьОбъект();
        Объект.Заблокирован = Истина;
        Объект.Записать();
    КонецЕсли;
КонецПроцедуры
```


### КОД МОДУЛЯ ПРИЛОЖЕНИЯ:
```
Процедура ПриНачалеРаботыСистемы()
	ОткрытьФорму("ОбщаяФорма.ФормаАвторизации");
КонецПроцедуры
```


---

# МОДУЛЬ 5
### ТЕХНИЧЕСКАЯ ДОКУМЕНТАЦИЯ.


---

# МОДУЛЬ 6
### НАСТРОЙКА ПЕРЕМЕННЫХ СРЕД
ДЛЯ ПОЛЬЗОВАТЕЛЯ И СИСТЕМЫ: JAVA_HOME, PROGRAM FILES\JAVA\JDK

### POSTMAN
GET - LOCALHOST:4444/TRANSFERSIMULATOR/FULLNAME

### СОЗДАНИЕ ОБРАБОТКИ "ВАЛИДАЦИЯ ДАННЫХ" И ФОРМЫ
РЕКВИЗИТЫ ФОРМЫ - ОТПРАВЛЯЕМЫЕ ДАННЫЕ И ПОЛУЧЕННЫЕ ДАННЫЕ
КОМАНДЫ - ОТПРАВИТЬ РЕЗУЛЬТАТ ТЕСТА И ПОЛУЧИТЬ ДАННЫЕ

### СОЗДАНИЕ ЭЛЕМЕНТОВ ФОРМЫ
ГРУППЫ БЕЗ ОТОБРАЖЕНИЯ ПОЛУЧЕНИЕ И ОТПРАВКА
ГРУППА 1 - КНОПКА ПОЛУЧИТЬ ДАННЫЕ И РЕКВИЗИТ ПОЛУЧЕННЫЕ ДАННЫЕ
ГРУППА 2 - КНОПКА ОТПРАВИТЬ РЕЗУЛЬТАТ ТЕСТА И РЕКВИЗИТ ОТПРАВЛЕННЫЕ ДАННЫЕ

### СВОЙСТВА ЭЛЕМЕНТОВ РЕКВИЗИТОВ
ПОЛОЖЕНИЕ ЗАГОЛОВКА "НЕТ"

### СОЗДАНИЕ ФАЙЛА DOCX
***ТАБЛИЦА***
ДЕЙСТВИЕ	ОЖИДАЕМЫЙ РЕЗУЛЬТАТ	РЕЗУЛЬТАТ (УСПЕШНО/НЕ УСПЕШНО)
ВВЕСТИ ЗАПРЕЩЕННЫЙ СИМВОЛ *	СООБЩЕНИЕ О ВВЕДЕННОМ СПЕЦ СИМВОЛЕ
ВВЕСТИ ЗАПРЕЩЕННЫЙ СИМВОЛ )	СООБЩЕНИЕ О ВВЕДЕННОМ СПЕЦ СИМВОЛЕ

### СОЗДАНИЕ ЗАКЛАДОК ДЛЯ ПЕРВОГО И ВТОРОГО ПОЛЯ РЕЗУЛЬТАТА

### КОД МОДУЛЯ ФОРМЫ ОБРАБОТКИ
```
&НаКлиенте
Процедура ПолучитьДанные(Команда)
	АдресРесурса = СокрЛП("/TransferSimulator/fullName");
	ВыполнитьЗапрос("GET", АдресРесурса, "");
КонецПроцедуры

&НаКлиенте
Процедура ВыполнитьЗапрос(ИмяМетода, АдресРесурса, СтрокаТелаЗапроса)       
	Сообщение = Новый СообщениеПользователю;
	СерверИсточник = "localhost";
	Попытка
	   HTTPСоединение = Новый HTTPСоединение(СерверИсточник,4444);	
	Исключение
		Сообщение.Текст="Не удалось соединиться с сервером: " + СерверИсточник;
		Сообщение.Сообщить();
		Сообщение.Текст=ОписаниеОшибки();
		Сообщение.Сообщить();
		Возврат;
	КонецПопытки;
	HTTPЗапрос = Новый HTTPЗапрос(АдресРесурса);
    Попытка
	    Результат=HTTPСоединение.ВызватьHTTPМетод(ИмяМетода, HTTPЗапрос);
		ЧтениеJSON=Новый ЧтениеJSON;
		ЧтениеJSON.УстановитьСтроку(Результат.ПолучитьТелоКакСтроку());
		ДанныеОтвета=ПрочитатьJSON(ЧтениеJSON,Ложь);
		ЧтениеJSON.Закрыть();
		ПолученныеДанные=ДанныеОтвета.value;	
	Исключение
		Сообщение.Текст=ОписаниеОшибки();
		Сообщение.Сообщить();
		Возврат
	КонецПопытки;	
КонецПроцедуры

&НаКлиенте
Процедура ОтправитьРезультатТеста(Команда)
	// Вставить содержимое обработчика.  
	Если СтрНайти(ПолученныеДанные,"*")<>0 Тогда
		РезультатПроверки="Не успешно";
		ОтправленныеДанные="ФИО содержит запрещенный символ *";
	Иначе
		РезультатПроверки="Успешно";
		ОтправленныеДанные="ФИО не содержит запрещенных символов";
	КонецЕсли;
	ОтправитьРезультатВWord(РезультатПроверки, 1);
	Если СтрНайти(ПолученныеДанные,")")<>0 Тогда
		РезультатПроверки="Не успешно";
		ОтправленныеДанные=ОтправленныеДанные+Символы.ПС+"ФИО содержит запрещенный символ )";
	Иначе
		РезультатПроверки="Успешно";
		ОтправленныеДанные=ОтправленныеДанные+Символы.ПС+"ФИО не содержит запрещенных символов";
	КонецЕсли;
	ОтправитьРезультатВWord(РезультатПроверки, 2);	

КонецПроцедуры            

&НаКлиенте
Процедура ОтправитьРезультатВWord(РезультатПроверки, НомерТеста)
	Попытка
		ОбъектWord=Новый COMОбъект("Word.Application");
	Исключение
		Сообщить("Ошибка при запуске Word" + ОписаниеОшибки(), СтатусСообщения.Внимание);
	КонецПопытки;
	ОбъектWord.Documents.Add("C:\Users\Admin\Desktop\ТестКейс.docx");
	ШаблонWord=ОбъектWord.ActiveDocument;
	ШаблонWord.Bookmarks("Тест"+НомерТеста).Select();
	ШаблонWord.Application.Selection.Text=РезультатПроверки;
	ШаблонWord.SaveAs("C:\Users\Admin\Desktop\ТестКейс.docx");
	ШаблонWord.Close();
КонецПроцедуры
```
